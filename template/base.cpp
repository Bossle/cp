#include <bits/stdc++.h>
using namespace std;

#ifdef BOSSLE_DEBUG
  #include "log.cpp"
#else
  #define LOG(...)
#endif

void solve();

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  solve();
}
